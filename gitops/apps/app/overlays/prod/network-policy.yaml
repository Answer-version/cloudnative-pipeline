apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: cloudnative-app
  labels:
    app: cloudnative-app
    app.kubernetes.io/name: cloudnative-app
spec:
  podSelector:
    matchLabels:
      app: cloudnative-app
  policyTypes:
    - Ingress
    - Egress
  ingress:
    - from:
        - namespaceSelector:
            matchLabels:
              name: ingress-nginx
      ports:
        - protocol: TCP
          port: 8080
  egress:
    - to:
        - podSelector: {}
      ports:
        - protocol: TCP
          port: 8080
    - to:
        - namespaceSelector: {}
      ports:
        - protocol: TCP
          port: 53
        - protocol: UDP
          port: 53
