apiVersion: v1
kind: ConfigMap
metadata:
  name: app-config
  labels:
    app: cloudnative-app
    app.kubernetes.io/name: cloudnative-app
data:
  APP_NAME: cloudnative-app
  APP_VERSION: "1.0.0"
  DEFAULT_LOG_LEVEL: info
  DEFAULT_TIMEOUT: 30s
